extends layout

block content
    ul.list-group
        each val in articles
            li.list-group-item.article-link
                =val.title
                =" - " + val.author
                =" " + val.creation_date